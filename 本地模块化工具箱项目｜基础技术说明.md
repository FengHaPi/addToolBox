# 本地模块化工具箱项目｜基础技术说明

## 一、项目目标

这是一个 **Windows 本地模块化工具箱**。

核心目标：

- 稳定
- 启动快
- 主程序低占用
- 功能可拆卸
- 模组独立安装 / 卸载
- 主题可更换
- UI 风格统一
- 后续方便持续增加新工具
- 尽量避免某个功能出问题导致整个程序崩溃

核心思想：

> 主程序只负责提供“箱子”，具体工具通过模组安装。

例如以后可以安装：

- 图片工具
- 视频工具
- 文件处理
- Excel 工具
- 文本工具
- AI 工具
- 下载工具
- 编码转换
- 游戏辅助工具
- 学习工具
- 工作统计工具
- 娱乐功能

---

# 二、基础技术栈

## C#

项目主要编程语言。

负责：

- 程序逻辑
- 文件处理
- 模组系统
- 配置读取
- UI 交互
- 网络请求
- 数据处理
- Windows 系统功能

后续绝大多数模组也可以直接使用 C# 开发。

---

## .NET 10

C# 程序运行的基础平台。

可以理解为整个程序的“发动机”。

负责提供大量现成功能，例如：

- 文件读写
- JSON
- 网络请求
- 多线程
- 压缩解压
- 进程管理
- 加密
- 日志基础
- 插件加载
- Windows 系统接口

原则：

> .NET 自带的功能能解决，就尽量不额外安装第三方库。

这样可以减少依赖，提高稳定性。

---

# 三、WPF

Windows 桌面 UI 框架。

负责所有可视界面，例如：

- 主窗口
- 侧边栏
- 工具卡片
- 设置页面
- 模组管理
- 搜索框
- 弹窗
- Toast 通知
- 按钮
- 输入框
- 数据列表
- 动画
- 主题

选择 WPF 的主要原因：

- Windows 上非常成熟
- 性能开销低
- 不需要浏览器内核
- C# 原生配合
- UI 自定义自由度高
- 很适合长期运行的本地工具
- 第三方依赖少

项目目前主要针对 Windows，因此暂时不考虑跨平台带来的额外复杂度。

---

# 四、CommunityToolkit.Mvvm

NuGet：

`CommunityToolkit.Mvvm`

作用：

帮助管理 UI 和程序逻辑之间的关系。

主要负责：

- ViewModel
- 属性变化通知
- Command
- 按钮事件
- 页面状态
- UI 数据绑定

例如：

用户点击：

`开始转换`

不是直接让按钮自己处理所有逻辑，而是：

`按钮 → Command → ViewModel → 功能服务`

这样以后项目变大以后不会所有代码都堆在 UI 里面。

主要价值：

> 让代码结构保持干净。

---

# 五、Microsoft.Extensions.DependencyInjection

NuGet：

`Microsoft.Extensions.DependencyInjection`

简称：

DI，依赖注入。

负责统一管理程序中的各种服务。

例如：

- 日志服务
- 配置服务
- 模组服务
- 主题服务
- 通知服务
- 文件服务
- 数据库服务

以后一个模组需要日志：

不需要自己创建一个日志系统。

直接向主程序申请：

`ILogger`

需要配置：

直接申请：

`IConfigurationService`

主要价值：

> 降低模块之间的硬连接。

方便以后替换、测试和扩展。

---

# 六、Microsoft.Extensions.Logging

NuGet：

`Microsoft.Extensions.Logging`

负责统一日志接口。

以后程序出现：

- 模组加载失败
- 文件读取失败
- 网络错误
- 数据库异常
- 程序崩溃
- 模组崩溃

都可以记录。

例如：

`Logs/2026-08-31.log`

以后排查 Bug 时，不需要猜：

> “它怎么突然炸了？”

直接查日志。

这是稳定性设计的重要组成部分。

---

# 七、System.Text.Json

.NET 自带。

不需要额外安装。

负责 JSON。

主要用于：

- 软件配置
- 模组配置
- 模组清单
- 用户设置
- 主题信息
- 缓存信息

例如：

`manifest.json`

可以记录：

- 模组名称
- 模组 ID
- 作者
- 版本
- 最低主程序版本
- 权限
- 入口 DLL
- 图标
- 描述

以后 `.kbmod` 模组安装系统会大量使用它。

---

# 八、AssemblyLoadContext

.NET 自带。

这是整个“模组系统”的核心之一。

负责动态加载：

`Module.dll`

以后主程序启动时可以：

`扫描 Modules`
↓
`读取 manifest.json`
↓
`加载对应 DLL`
↓
`注册模组`
↓
`显示到工具箱`

用户没有安装某个模组：

主程序就完全不用加载它。

可以实现：

- 安装模组
- 启用模组
- 禁用模组
- 卸载模组
- 独立模组依赖
- 模组版本管理

---

# 九、AssemblyDependencyResolver

.NET 自带。

配合 AssemblyLoadContext 使用。

主要解决：

> 不同模组使用不同版本第三方库的问题。

例如：

图片模组需要：

`Library A 2.0`

另一个模组需要：

`Library A 3.0`

尽量让两边互不干扰。

这是插件架构里非常重要的一层隔离。

---

# 十、ResourceDictionary

WPF 自带。

负责主题和 UI 样式。

以后不会在每个按钮里面直接写：

`背景 = 白色`

而是定义：

- Background
- CardBackground
- AccentColor
- TextPrimary
- TextSecondary
- BorderColor
- CornerRadius
- Spacing

主题只需要替换这些资源。

例如以后可以出现：

- 默认浅色
- 深色
- 樱花主题
- 像素主题
- 赛博主题
- 自定义主题

未来可以设计自己的：

`.kbtheme`

主题文件。

---

# 十一、System.IO.Compression

.NET 自带。

负责 ZIP 压缩和解压。

未来主要用于模组安装。

例如：

`ImageTools.kbmod`

实际上内部可以是：

```text
manifest.json
ImageTools.dll
icon.png
assets/
dependencies/
```

用户双击 `.kbmod`：

主程序自动：

检查
→ 解压
→ 安装
→ 注册
→ 启用

用户不需要自己复制 DLL。

---

# 十二、项目后续可以发展出的能力

## 模组系统

未来可以做到：

- 本地安装模组
- 拖拽安装
- 双击 `.kbmod` 安装
- 启用 / 禁用
- 卸载
- 更新
- 版本检查
- 依赖检查
- 权限提示
- 模组商店

---

## 主题系统

可以做到：

- Light
- Dark
- 跟随系统
- 自定义主题
- 第三方主题
- 一键安装 `.kbtheme`
- Accent Color
- 圆角风格
- UI 密度
- 动画强度

---

## 工具系统

以后每个工具都可以成为独立模块。

例如：

```text
Modules/
├─ ImageTools
├─ VideoTools
├─ FileTools
├─ TextTools
├─ ExcelTools
├─ AITools
└─ GameTools
```

不用为了增加一个功能修改整个主程序。

---

## 重型工具隔离

以后像：

- FFmpeg
- AI 模型
- OCR
- 视频编码
- 大型图片处理
- 第三方命令行程序

可以不直接运行在主程序内部。

而是：

`主程序`
↓
`Worker.exe`
↓
`执行任务`

这样 Worker 崩溃时：

主程序尽量继续运行。

---

# 十三、暂时不要安装的东西

目前不要因为“以后可能用到”就提前安装：

- FFmpeg
- OpenCV
- ImageSharp
- ClosedXML
- SQLite
- OCR
- AI SDK
- Markdown 库
- 浏览器组件
- WebView
- 大型 UI 框架

等真正开发对应模组时再选择。

原则：

> 功能需要什么，功能自己带什么。

而不是：

> 主程序把所有东西都装进去。

---

# 十四、当前最基础的外部依赖

项目刚开始只需要重点考虑：

```text
CommunityToolkit.Mvvm
Microsoft.Extensions.DependencyInjection
Microsoft.Extensions.Logging
```

其余大量基础能力直接使用：

```text
.NET 10
WPF
System.Text.Json
System.IO.Compression
AssemblyLoadContext
AssemblyDependencyResolver
ResourceDictionary
```

这些属于 .NET / WPF 本身。

---

# 十五、项目核心原则

### 1. Core 保持轻量

核心程序不要塞图片、视频、Excel、AI 等专用依赖。

### 2. 模组自己管理功能依赖

图片库属于图片模组。

Excel 库属于 Excel 模组。

视频库属于视频模组。

### 3. 优先使用 .NET 自带能力

能少一个第三方依赖就少一个。

### 4. 稳定优先于极限体积

不为了省几十 MB 引入大量兼容问题。

### 5. UI 由宿主统一规范

模组可以扩展功能，但尽量使用统一 UI 组件。

### 6. 从第一天设计成模块化

不能先把全部功能写进主程序，以后再强拆插件。

### 7. 依赖按需增加

不是开工前安装几十个库。

正确方式：

`需要功能`
→ `判断 .NET 是否自带`
→ `没有再找成熟依赖`
→ `尽量只安装到对应模组`

---

## 一句话总结

这个项目的技术路线是：

> **C# + .NET 10 + WPF 做一个轻量稳定的 Windows 宿主，通过独立模组不断增加功能，通过主题系统改变外观，主程序本身始终保持干净、稳定和低占用。**