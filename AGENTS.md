# addToolBox AI Development Rules

## 1. Bootstrap 特例

当前首次初始化时，ARCHITECTURE.md 和 AGENTS.md 原本不存在，因此首次创建这两个文件属于唯一 Bootstrap 特例。

两个文件创建完成以后，必须立即重新读取：

- ARCHITECTURE.md
- AGENTS.md

从此之后，任何代码或架构修改前都必须先阅读这两个文件。

## 2. 修改前规则

任何正式开发任务开始前：

1. 阅读 ARCHITECTURE.md
2. 阅读 AGENTS.md
3. 检查当前相关代码
4. 明确当前任务目标
5. 判断需求是否与架构冲突

如果需求与 ARCHITECTURE.md 冲突，停止实施并报告。禁止偷偷绕过架构。

非纯阅读任务在正式修改前，应明确一个最小执行计划：

- 本任务目标
- 明确不做什么
- 预计修改哪些文件
- 是否触及高风险区域
- 计划如何验证

对于普通、低风险且未超预算的任务，完成上述检查后可以直接实施，不需要每一步都等待用户确认。

如果触及高风险修改、超出修改预算、系统环境变化、Git 写操作或不明确的架构变化，必须按对应规则判断当前请求是否已经构成明确批准；未被当前请求明确覆盖时，停止并等待批准。

## 3. 根因优先

出现 Bug 时优先寻找根因。

禁止通过以下方式掩盖问题：

- 增加无必要兼容层
- 重复状态
- 临时旁路
- 多层 fallback
- 新增另一套逻辑绕开旧逻辑
- 用特殊判断不断堆补丁

如果根因位于架构本身，应明确报告，而不是继续堆补丁。

## 4. 修改预算

普通开发任务默认最多修改 5 个生产文件。

“生产文件”不限于 `.cs`。所有可能改变构建或运行行为的文件都计入，例如：

- `.cs`
- `.xaml`
- `.csproj`
- `.props`
- `.targets`
- 运行时配置
- 构建配置
- 资源文件
- manifest
- 数据 Schema

以下通常不计入数量预算：

- 测试文件
- 用户明确要求同步维护的文档
- `bin/`
- `obj/`
- 自动生成文件

“不计入数量预算”不代表可以无限修改。测试和文档仍必须严格属于当前任务范围。

如果预计需要超过 5 个生产文件，且当前请求没有明确列出并批准该范围，必须在修改前停止并报告：

- 为什么需要扩大范围
- 涉及哪些文件
- 是否代表架构问题
- 是否可以拆成多个更小任务

得到用户明确批准后才能继续。当前请求已经具体列出超预算文件和必要范围时，可以视为本次任务的明确批准，但不得扩大范围。不得先修改完再解释为什么超过预算。

## 5. 高风险修改审批

高风险修改至少包括：

- AddToolBox.Core
- AddToolBox.SDK
- 公共 Module Contract
- 公共 API / 公共数据格式
- 核心状态模型
- Single Source of Truth 的归属
- 项目依赖方向
- `.csproj` 中的重要构建行为
- `Directory.Build.*`
- `Directory.Packages.*`
- 持久化数据格式或 Schema
- AGENTS.md
- ARCHITECTURE.md（包括其中的架构决策）

修改前必须明确说明：

- 为什么现有设计不能满足需求
- 为什么不能在更局部范围解决
- 涉及哪些文件
- 会影响哪些已有能力
- 是否影响兼容性
- 是否产生迁移成本
- 是否存在更小的方案

如果当前请求没有明确覆盖该高风险修改，报告后必须停止。未经用户明确批准，不得实施高风险修改。

AGENTS.md 和 ARCHITECTURE.md 不得在普通功能开发、Bug 修复或重构任务中顺手修改。只有用户明确提出治理或架构变更时才能修改。

不得为了完成当前代码任务而削弱、删除或绕过治理规则。不得通过修改 AGENTS.md 使原本不允许的操作变得允许。

不得为了让当前实现“符合架构”，先写代码，再反向修改 ARCHITECTURE.md。ARCHITECTURE.md 是代码应遵守的约束，不是失败实现的橡皮泥。

如果确实需要改变架构，必须先提出架构变更理由，获得明确批准，先更新架构决策，再进入独立的后续代码任务。禁止因为单个功能方便而随意扩大核心协议。

### 明确批准的作用域

本节适用于所有需要用户明确批准的操作。用户批准必须具体、可识别。

如果用户当前请求已经明确要求某项高风险操作，并明确了目标和范围，该当前请求可以视为对该明确操作的批准，不需要对同一范围重复请求确认。

批准遵守最小作用域：

- 只适用于当前任务
- 只适用于明确列出的行为
- 只适用于必要的文件和范围
- 不得扩展到相邻功能
- 不得自动延续到后续任务
- 不得因为用户曾经批准过一次而视为永久批准

以下模糊表达不能自动视为新的高风险批准：

- “继续”
- “修一下”
- “做完它”
- “处理一下”
- “优化一下”

如果需要批准的高风险行为没有被当前请求明确覆盖，必须先报告并等待批准。

## 6. 自动修复次数限制

同一个失败问题最多允许 2 次代码修改尝试。

- 第 1 次代码修改失败后，只有获得新的、可验证的证据，才允许进行第 2 次修改。
- 第 2 次代码修改仍失败，必须立即停止。
- 禁止进行第 3 次代码修改尝试。
- 禁止通过换名字、换实现路径或拆成子问题规避次数限制。
- 单纯重复执行相同 build/test 命令用于确认结果，不算新的代码修改尝试，但禁止无意义重复运行。
- 如果失败原因发生了实质变化，必须说明为什么这是新的独立问题，而不是自行重置计数。

第 2 次代码修改仍失败后必须报告：

- 当前失败现象
- 已验证事实
- 两次修改分别做了什么
- 得到了什么新证据
- 当前怀疑根因
- 尚不确定的信息
- 建议下一步检查内容

禁止陷入 regenerate / retry / fallback 循环。

## 7. 禁止静默 fallback

不得未经设计自行增加：

```text
A 失败
→ 尝试 B
→ B 失败
→ 使用默认值
→ 猜测结果
```

如果存在合法降级策略，必须是明确的架构设计，并具有可观察的日志或状态。

## 8. 禁止吞异常

禁止：

- 空 catch
- catch(Exception) 后无记录继续运行
- 将未知异常伪装成成功

应该：

- 在合适边界记录
- 保留真实失败原因
- 必要时隔离失败功能

## 9. Single Source of Truth

不得为同一个权威状态创建多个独立来源。

缓存不是第二个权威状态。UI 状态不是第二个业务真相。文件存在也不能自动成为另一个权威状态，除非架构明确规定它就是来源。

如果无法确定权威来源，先停止并澄清设计。

## 10. 测试规则

未来存在测试后，测试失败时默认先检查生产代码。禁止单纯为了让测试变绿而修改正确测试。

只有确认以下情况之一，才允许修改测试，并说明原因：

- 需求正式变化
- 测试本身存在错误

为了让测试变绿，禁止：

- 删除正确测试
- 删除关键断言
- 放宽断言到失去意义
- 给失败测试添加 Skip / Ignore
- 屏蔽测试程序集
- 隐藏测试失败
- 无依据增加 retry
- 无依据增加超长 sleep
- 关闭分析器或 warning 只为隐藏当前问题
- 修改测试数据让错误路径不再触发

如果认为测试错误，必须先证明理由。

构建失败不得宣称任务完成。测试失败不得宣称任务完成。

## 11. 禁止任务外重构

禁止：

- “顺手整理一下”
- “顺便重构”
- “顺便换个库”
- “顺便改命名”
- “顺便升级依赖”

如果与当前任务没有直接关系，不修改。发现其他问题可以在最终报告中单独指出。

## 12. 第三方依赖规则

新增、替换或升级第三方依赖前必须报告：

- 包名
- 目标版本
- 用途
- 为什么 .NET / WPF 自带能力不够
- 为什么依赖应该位于当前层
- 是否应该只属于某个 Module
- 对体积、启动时间、内存、兼容性、维护的影响
- 是否有更轻量或更成熟的替代方案

如果当前请求没有明确覆盖对应依赖操作，报告完成后必须停止。未经用户明确批准：

- 不得安装新的 NuGet
- 不得修改现有依赖版本
- 不得替换第三方库
- 不得批量升级
- 不得更换 UI 框架、序列化方案、数据库方案等基础技术

## 13. 不确定时停止

如果需求、业务规则、架构含义不明确，不得自行猜测重要规则。应停止并提出具体问题或报告不确定项。

## 14. 完成定义

只有满足任务明确验收条件后才能宣称完成。

任务结束前必须完成第 23 节 Change Documentation 评估。应更新的变更记录属于同一任务的 Definition of Done；尚未更新时，不得宣称任务完成。

最终报告至少说明：

- 修改了什么
- 为什么修改
- 实际验证了什么
- build 是否通过
- tests 是否通过（如果存在）
- 是否存在 warning
- 是否存在未解决问题
- 是否偏离原计划

禁止把“理论上应该可以”描述成“已经验证成功”。

最终报告必须明确区分：

- 已实现
- 已通过自动测试验证
- 已通过 build 验证
- 已实际运行验证
- 仅静态检查
- 尚未验证

只执行 `dotnet build` 成功，不得写成“应用已经运行正常”。没有实际启动窗口，不得写成“UI 已验证”。

最终报告必须列出实际执行的关键验证命令，例如：

- `dotnet build ...`
- `dotnet test ...`

必须准确描述每条命令的验证范围。如果只执行单个测试、测试过滤器、单个项目 build 或局部静态检查，只能报告对应的局部结果。

禁止将“筛选出的 3 个测试通过”描述为“全部测试通过”。

未来 solution 建立以后，生产代码发生修改时，任务结束前原则上应进行 solution 级 build。如果没有执行，必须明确说明原因，不得默认为已验证。

## 15. 文档真实性

任何文档必须区分：

- 已实现
- 已验证
- 计划中
- 尚未实现

禁止把路线图写成现状。

## 16. Git 默认只读与破坏性操作保护

任何任务开始前，如果已经存在 Git 仓库，应查看 `git status`。

如果发现与当前任务无关的未提交修改：

- 不得覆盖
- 不得删除
- 不得恢复
- 不得 reset
- 不得擅自包含进当前修改

必要时停止并报告。

默认情况下，只允许不会改变仓库状态的 Git 查询，例如：

- `git status`
- `git diff`
- `git log`
- `git show`
- `git branch --show-current`
- `git rev-parse`

除非用户当前请求明确授权，否则禁止任何会改变工作树、暂存区、分支、标签、Git 历史或远程仓库的 Git 操作，包括但不限于：

- `git add`
- `git commit`
- `git stash`
- `git checkout`（会改变文件或分支时）
- `git switch`
- `git merge`
- `git cherry-pick`
- `git rebase`
- `git reset`
- `git clean`
- `git push`
- `git pull`
- force push
- 其他改写 Git 历史的操作
- 删除用户已有分支
- 恢复或覆盖用户未提交内容

不得依赖命令黑名单判断是否需要批准。判断标准是：“这个命令是否会改变 Git 或工作树状态”。如果会，默认需要当前任务的明确授权。

如果当前请求没有明确覆盖，执行以下操作前必须报告影响范围并等待用户明确批准：

- 删除已有文件
- 覆盖已有文件的大部分内容
- 大范围移动
- 大范围重命名
- 批量替换
- 删除数据或配置

禁止通过删除失败代码、删除测试、删除数据或回退需求来制造“成功”。

## 17. Bug 证据规则

修复 Bug 前，必须先尽可能建立以下证据之一：

- 可重复的失败步骤
- 失败测试
- 日志、异常、调用链等明确证据

禁止按以下方式处理：

```text
“看起来可能是这里”
→ 直接修改生产代码
→ 编译通过
→ 宣称修复
```

如果无法稳定复现，应说明：

- 已知现象
- 可以确认的事实
- 无法确认的部分

然后控制修改范围，不得进行大范围猜测式修复。

## 18. 禁止提前抽象

禁止为了“以后可能会用”提前创建：

- Manager
- Factory
- Repository
- Provider
- Adapter
- Wrapper
- Compatibility Layer
- BaseXXX
- 通用接口
- 空 Service
- 第二套状态模型

只有出现当前真实需求时才增加抽象。宁可保持简单代码，也不要制造尚未被需求验证的架构。

## 19. 最终 Diff 自审

任何修改任务完成前必须检查最终修改范围。

如果存在 Git，至少检查 `git diff`、`git diff --stat` 或等价信息。

必须确认：

- 只修改计划内文件
- 没有意外删除
- 没有任务外重构
- 没有旧项目名称
- 没有临时调试代码
- 没有注释掉旧实现来绕过问题
- 没有无需求 TODO / FIXME
- 没有生成文件被错误纳入
- 没有偷偷修改依赖或项目配置

发现偏离时必须先修正，不能直接宣称完成。

## 20. 仓库和系统环境边界

默认修改范围仅限当前 addToolBox 仓库根目录。

未经用户明确批准，不得：

- 修改仓库之外的文件
- 修改用户目录中的其他配置
- 修改系统环境变量
- 修改 PATH
- 修改 Windows 注册表
- 修改系统服务
- 修改系统策略
- 安装或卸载系统软件
- 安装或卸载 .NET workload
- 执行 `winget`、`choco`、`scoop` 等安装操作
- 修改 Visual Studio 安装
- 下载并执行未知二进制文件或脚本
- 修改全局 Git 配置
- 修改全局 NuGet 配置

如果完成任务确实需要系统级变化，必须先说明：

- 为什么需要
- 会修改什么
- 是否可逆
- 是否有项目内替代方案

如果当前请求没有明确覆盖该系统级变化，说明后必须停止并等待用户明确批准。即使当前请求已明确批准，也只能在批准的最小范围内执行。

## 21. 自动生成文件保护

以下内容不得作为正常修复目标手动编辑：

- `bin/`
- `obj/`
- 自动生成的 `*.g.cs`
- 自动生成的中间文件
- 编译输出
- 第三方生成产物

如果这些内容异常，应找到它们对应的源文件、项目配置、生成规则或真实根因。

禁止修改生成结果来制造“已经修复”。

## 22. 敏感信息与安全底线

禁止：

- 将 API Key、Token、密码、私钥写入源码
- 将秘密写入日志
- 将秘密加入测试快照
- 将秘密提交到 Git
- 输出与当前任务无关的凭据
- 为解决网络问题关闭 TLS 验证
- 禁用证书校验
- 将真实秘密硬编码为默认值

如任务需要凭据，优先使用安全的外部配置方式。

不得自行创建真实凭据。不得因为缺少凭据而伪造“已经验证成功”。

## 23. Change Documentation

### 记录职责与触发条件

开始功能、行为、架构或重要研究任务时，读取 `CHANGELOG.md` 和 `docs/PROJECT_HISTORY.md` 中的相关记录，确认既有决策、退役方向与当前状态。

任何任务结束前都必须评估这两个文件是否需要更新。出现以下任一情况时，必须进行对应的变更记录维护：

- 新增或删除用户可见功能
- 功能进入 Deprecated / Retired 状态
- 重要交互或产品行为改变
- 架构方向改变
- 新增或移除项目、增加/移除/替换/升级依赖
- Module / SDK Contract 或其他公共 API 改变
- 数据、配置或持久化格式改变
- 重要技术研究的结论影响后续产品方向
- 对产品或开发者有实际意义的 Bugfix

“自动记录”指每个开发者或 AI 必须在当前任务内完成评估、必要更新和最终报告，不依赖临时记忆。此规则本身不代表已经安装日志生成脚本、Git hook 或 CI 检查。

### CHANGELOG.md：有意义的变化

用简短条目记录产品或开发者需要知道的变化，按 `Added`、`Changed`、`Removed`、`Fixed`、`Research / Technical Milestone`、`Deprecated / Retired` 分类。

例如新增 Mouse Wheel Zoom、Pan 按键改变、移除 Automatic Compaction、修复 Resize 改写 WorldPosition。普通但有用户影响的 Bugfix 通常只更新 CHANGELOG。

未正式 Release 时维护 `Unreleased`，早期阶段保留在 `Historical Milestones`；正式 Release 后按真实 Release 版本切分，不虚构版本或发布日期。

### docs/PROJECT_HISTORY.md：重要演化与决策

以下情况必须更新详细历史：

- 重要功能第一次建立或正式取消
- 产品方向改变，或原架构被另一架构替代
- 大型研究虽未上线，但结论影响项目方向
- 需要保留原因、替代方案、后续影响，以回答未来开发者“为什么当初不这样做”

只记录 Milestone / Decision / Retired Direction / Significant Research，说明做了什么、为什么、结果、状态和证据。退役记录保留原因及研究价值，不把旧方向继续列为当前功能。

不得按每个 Commit 机械追加长文。纯格式化、私有变量改名、无行为变化的内部重构、普通注释、临时 Probe、单次 build 命令等，通常不更新两份日志；若 Probe 形成影响产品方向的重要结论，则记录结论而非执行流水。

### 同任务更新与授权边界

必要的日志更新属于当前任务的 Definition of Done，必须与对应代码或决策在同一任务中完成。不得以“以后再补日志”结束应记录的任务。

这两份日志中与当前任务直接相关的必要维护，属于本制度要求的常规文档范围；仍受任务范围、文档真实性和未提交工作保护约束。日志维护不授权修改 `AGENTS.md`、`ARCHITECTURE.md`、生产代码、依赖或系统环境，也不授权 Git 写操作。

记录架构方向不能替代架构审批；`ARCHITECTURE.md` 仍是架构约束的权威文件。发现既有文档与代码不一致时，明确记录差异和证据，不通过历史文件重新定义架构或绕过第 5、12、16、20 节规则。

### 证据、状态与日期

- 依据真实 Git、仓库文件、当前工作树和项目所有者明确提供的事实，不从 Commit 标题猜实现。
- 已提交历史使用经验证的 short hash + commit subject，必要时附完整 hash；历史时间轴采用 Git Commit Date，并注明时区。
- 当前未提交工作标为 `Uncommitted / In progress`，不得虚构未来 Hash，也不得描述为已发布。
- 区分已实现、仅静态核实、已 build、已测试、已运行、Planned / Current direction、Research、Superseded、Retired；build 成功不能证明交互或研究结果。
- 不能精确定位早期决策时，标为 `Owner-confirmed design decision; exact commit not isolated.`。日期只能取真实提交日期或明确的当前记录日期，不能根据文件时间猜发生日期。
- 所有者提供的研究数字须注明来源；缺少原始报告、测试输入或复现证据时明确说明，不把历史报告写成本次测试结果。
- 有 Removal 决定但代码未完成移除时，记录 `Decision: retiring / removal in progress`；调用链移除、算法源码保留和整个功能删除应分别说明。
- 不为补日志 amend、rebase、reset、改写旧提交、打 Tag、自动暂存或提交。

### 完成检查与最终报告

检查两份日志之间的状态、日期和链接是否一致，并对照当前代码及 README；仅维护有意义的条目，避免重复。修改后执行 `git diff --check`，并检查未跟踪的新文档；生产修改的 build / test 要求仍按第 14 节执行。

每次任务最终报告必须包含以下两行，并给出具体原因；`NO` 不能用于推迟本应完成的更新：

```text
Change documentation:
- CHANGELOG updated: YES/NO + reason
- PROJECT_HISTORY updated: YES/NO + reason
```
