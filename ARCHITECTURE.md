# addToolBox Architecture

## 1. 项目定位

addToolBox 是一个 Windows 本地模块化工具箱。

项目目标：

- 稳定性优先
- 可维护性优先
- 宿主自身低资源占用
- 功能可拆卸
- 模组可独立安装、禁用、卸载
- UI 风格统一
- 主题可扩展
- 后续允许持续增加工作、学习、娱乐及其他工具
- Windows 为当前主要目标平台

当前技术路线：

- C#
- .NET 10
- WPF

技术路线已经落地为 `AddToolBox.sln` 与下述五个项目；具体 Module 系统尚未实现。

## 2. 核心设计思想

主程序是宿主。具体工具不应该不断堆进主程序核心，未来具体功能主要通过 Module 扩展。

宿主负责公共能力，例如：

- 生命周期
- 模组管理
- UI 框架
- 主题
- 配置基础能力
- 日志基础能力
- 公共服务

具体模组负责自己的业务功能。例如未来可能存在：

- 图片工具模组
- 视频工具模组
- Excel 工具模组
- 文件工具模组
- AI 工具模组

这些只是示例和计划，不表示已经实现。

## 3. 项目分层

当前已建立：

- AddToolBox.App
- AddToolBox.Core
- AddToolBox.SDK
- AddToolBox.Infrastructure
- AddToolBox.UI

### AddToolBox.App

WPF 应用入口和最终组合层；当前包含 Shell、World Canvas Workspace 与 Tool Host 基础。

### AddToolBox.Core

存放稳定的核心规则和模型；当前仅有不依赖 WPF 的 `ToolDefinition(Id, DisplayName, IconKey)`。

Core 不允许因为某一个具体工具的需求不断膨胀。

### AddToolBox.SDK

项目骨架已建立；未来模组开发需要的公共契约尚未设计和实现。

SDK 应保持小、稳定，并尽量减少依赖。不能因为单个模组方便就随意修改 SDK。

### AddToolBox.Infrastructure

项目骨架已建立；文件、配置、日志、模组加载等基础设施仍为计划职责，尚未实现。

### AddToolBox.UI

WPF 类库骨架已建立；公共控件、样式、Design Tokens、主题等仍为计划职责，尚未形成公共 UI 系统。

项目存在不代表其全部计划职责已完成。当前项目引用仅为 App → Core，没有第三方 PackageReference。

## 4. 项目分层与引用原则

必须遵守：

- 禁止循环依赖
- SDK 不应该依赖 App、Infrastructure 或具体 Module
- Core 不允许依赖 App
- Core 不允许依赖具体 Module
- Core 不应该依赖具体图片、视频、Excel、AI 等业务库
- 具体 Module 不得反向成为 Core 的依赖
- App 作为组合入口可以负责连接各层
- 新增项目引用必须具有实际需求，不为了“以后可能需要”提前增加

后续项目引用继续按真实需求确定，不提前增加无用途的引用关系。

## 5. 模组原则

未来模组系统必须遵守：

- 宿主不能知道所有具体工具类型
- 未安装的模组不应成为宿主必需依赖
- 模组自己的业务依赖尽量留在模组内部
- 一个模组失败不应该轻易拖垮整个宿主
- 高风险或重型功能未来允许使用独立进程隔离
- 模组协议必须保持尽可能小而稳定
- 不提前设计尚未被真实需求验证的复杂插件接口

AssemblyLoadContext 和 AssemblyDependencyResolver 是计划使用的实现方向，目前尚未实现。

### 信任边界与进程隔离（计划原则）

AssemblyLoadContext 和 AssemblyDependencyResolver 主要解决程序集加载和依赖隔离问题，不应被描述为安全沙箱。

未来如果模组属于以下情况，应评估独立进程隔离：

- 不可信代码
- 高崩溃风险
- 高资源占用
- 调用大型外部程序
- 需要强隔离

具体安全模型、IPC、权限系统目前尚未设计和实现。不得声称项目已经具有插件安全沙箱。

## 6. 状态管理原则

重要状态必须有明确的 Single Source of Truth。禁止同一个权威状态同时由多个地方独立维护。

例如未来某个状态如果由 Registry 负责，则文件系统、缓存、UI 状态不得各自变成第二个权威来源。

未来配置、缓存、日志、文件系统状态、数据库状态必须明确各自职责：

- Cache 只能是可重建的派生数据，不应自动成为业务真相
- Log 用于记录事实，不作为正常业务状态数据库
- UI 状态不能反向成为持久化业务真相
- 存在多份持久化表示时，必须明确权威来源和同步规则

当前尚未确定具体持久化方案，不提前指定数据库或 Schema。

如果当前架构无法判断谁是权威来源，应先设计清楚，而不是增加 fallback。

## 7. 错误处理原则

禁止：

- 静默吞异常
- 用 catch 后什么都不做
- 新逻辑失败后偷偷切换到另一套旧逻辑
- 为了“看起来能运行”而掩盖真实错误

原则：

- 错误应该被明确记录
- 可以隔离失败功能
- 可以安全降级时必须有明确、已设计的规则
- 未经设计的隐式 fallback 不允许存在

## 8. 第三方依赖原则

优先使用 .NET / WPF 自带能力。

新增第三方依赖前必须确认：

1. 为什么需要它
2. .NET 自带能力为什么不足
3. 这个依赖应该属于宿主还是具体 Module
4. 是否有成熟、维护正常、更小的替代方案

禁止为了未来可能使用而提前安装依赖。

## 9. UI 与主题原则

WPF 作为当前 UI 框架。未来计划建立统一 UI 系统。

宿主计划提供：

- 公共控件
- 样式
- Design Tokens
- ResourceDictionary
- 主题规范

具体模组尽量遵守宿主 UI 规范。禁止每个模组随意建立完全不同的视觉体系。

主题系统计划基于 WPF ResourceDictionary 等成熟机制实现，目前尚未实现。

## 10. 稳定性原则

优先级：

稳定性
>
可维护性
>
性能
>
开发便利性
>
极限安装体积

不为了节省少量磁盘或内存，引入明显更高的架构复杂度和兼容性风险。

## 11. 公共契约兼容原则

以下内容是未来公共契约正式发布并被外部模组使用后的计划原则：

- SDK
- Module Contract
- manifest 格式
- 主题格式

届时对这些公共契约的变更将属于高风险兼容性修改，不得因为内部实现方便而随意破坏。

具体版本策略目前尚未设计，不提前虚构版本协议。

## 12. 架构变更治理

ARCHITECTURE.md 是项目架构事实和约束的权威文档，代码实现应遵守架构文档。

禁止先让代码绕过架构，再修改 ARCHITECTURE.md 使违规实现合法化。

如果真实需求证明现有架构需要变化，必须按以下顺序处理：

1. 说明现有架构为什么不满足需求
2. 提出最小架构变更
3. 说明影响范围和兼容性
4. 获得用户明确批准
5. 更新 ARCHITECTURE.md
6. 在独立后续任务中实施代码变更

不得在普通功能任务中顺手改变架构。

## 13. 当前状态

当前主体基线为 World Canvas Host Baseline；下一主要阶段为 Module System V0.1，尚未开始实现 Module 系统。

目前已经建立的是：

- 产品方向
- 技术路线
- 基础架构原则
- AI 开发治理原则
- 五项目 C# solution 与 WPF App
- Windows 11 风格 Shell、World Canvas / Viewport、Pan / Zoom / Reset View、Dynamic WorldExtent
- ToolDefinition、Tool 世界坐标交互、Layout Lock / Reset Layout 和 Tool Host 基础；默认 Workspace 为空

目前尚未实现：

- 模组加载
- 模组安装
- 主题系统
- 配置系统
- 日志系统
- 实际工具
- 安装程序

计划能力不得描述为已完成能力。
